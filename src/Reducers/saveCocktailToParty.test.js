import { saveCocktailToParty } from './saveCocktailToParty';

describe('saveCocktailToParty', () => {
  it.skip('should return the initial state', () => {
    // Setup
    const expected = null;

    // Execution
    const result = saveCocktailToParty(undefined, null);

    // Expectation
    expect(result).toEqual(expected);
  });
});